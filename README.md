# Nesttern — Intern housing & roommate matching

Full-stack starter scaffold for Nesttern (React + TypeScript frontend, Node/Express backend, PostgreSQL + Prisma).

See `backend/README.md` and `frontend/README.md` for per-app instructions.
